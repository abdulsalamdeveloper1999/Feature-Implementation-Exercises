name: tutorials_project
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter

  #provider state management
  provider: ^6.0.5

  #flutter bloc
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5

  #for api get post update
  http: ^1.1.0

  #sqflite
  path_provider: ^2.1.1
  sqflite: ^2.3.0

  #hive
  hive: ^2.2.3
  hive_flutter:

  supabase_flutter: ^1.10.22
  go_router: ^12.0.0
  shared_preferences: ^2.2.2
  cached_network_image: ^3.3.0

  #fade up animation plugin
  #animate_do: ^3.1.2
dev_dependencies:
  #hive
  hive_generator:
  build_runner:

  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/
    - assets/fadeup/
    - assets/images/
    - assets/screenshot/

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
